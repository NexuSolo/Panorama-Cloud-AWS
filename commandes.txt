multipass launch -n swarm-manager --disk 4G
multipass launch -n swarm-worker1 --disk 4G
multipass launch -n swarm-worker2 --disk 4G

multipass.exe ls            // copier les ip des instances dans le fichier inventory.ini

docker container run --rm -it ansible-container /bin/bash
cat ~/.ssh/id_rsa.pub       //copier la clé ssh dans le fichier authorized_keys des instances
nano ~/.ssh/authorized_keys

ansible-playbook -i inventory.ini playbook.yml